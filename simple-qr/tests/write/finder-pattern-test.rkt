#lang racket

(require rackunit/text-ui)

(require rackunit "../../write/finder-pattern.rkt")

(require racket/draw)

(define test-finder-pattern
  (test-suite 
   "test-finder-pattern"

   (test-case
    "test-draw-finder-pattern"
    
    (let ([qr (QR 

    )
   ))

(run-tests test-finder-pattern)

